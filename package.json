{
  "name": "hapi-serverless",
  "version": "1.0.0",
  "description": "Seed project using hapi and serverless for running on AWS",
  "main": "index.js",
  "scripts": {
    "start": "./node_modules/.bin/sls offline",
    "deploy": "./node_modules/.bin/sls deploy -v",
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "./node_modules/.bin/standard --fix"
  },
  "author": "Fredrik Hörte",
  "license": "ISC",
  "dependencies": {
    "hapi": "^16.1.1"
  },
  "devDependencies": {
    "serverless": "^1.12.1",
    "serverless-offline": "^3.13.5",
    "standard": "^10.0.2"
  }
}
